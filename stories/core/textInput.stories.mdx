import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { componentWrapperDecorator, moduleMetadata } from "@storybook/angular";
import { CommonModule } from "@angular/common";

import { LoadingSpinnerComponent } from "projects/instant-ui-library/src/lib/loading-spinner/loading-spinner/loading-spinner.component";
import { InstantUiLibraryComponent } from "projects/instant-ui-library/src/lib/root/instant-ui-library.component";

## Loading Spinner

Loading spinner should be used when needing to overlay and indicate an action is in progress.

[Source Code](https://github.com/chemscn/instant-ui/tree/main/projects/instant-ui-library/src/lib/loading-spinner/loading-spinner)

## Playground

<Meta
  title="Components/Loading Spinner"
  parameters={{
    layout: "centered",
  }}
  argTypes={{
    id: {
      description: "Unique identifier for component",
      type: { required: true },
      table: {
        type: { summary: "string" },
        defaultValue: { summary: "-" },
      },
      control: {
        type: null,
      },
    },
    variant: {
      description: "Color variant for spinner",
      table: {
        type: {
          summary: "primary | secondary | tertiary | highlight",
        },
        defaultValue: { summary: "primary" },
        control: {
          type: "text",
        },
      },
    },
    size: {
      description: "Size used for spinner",
      table: {
        type: { summary: "small | medium | large | xlarge" },
        defaultValue: { summary: "small" },
        control: {
          type: "text",
        },
      },
    },
  }}
/>

export const Template = (args) => ({
  props: {
    ...args,
  },
  template: `
   <iu-loading-spinner
   [id]="id"
   [variant]="variant"
   [size]="size"></iu-loading-spinner>
  `,
});

<Canvas>
  <Story
    name="Playground"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner [size]="size" [variant]="variant" id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "1",
      variant: "primary",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={"Playground"} />

## Variants

#

### Default

<Canvas>
  <Story
    name="Default"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "2",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Colors

### Primary

<Canvas>
  <Story
    name="Primary"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner variant="primary" id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "3",
      variant: "primary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Secondary

<Canvas>
  <Story
    name="Secondary"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner variant="secondary" id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "4",
      variant: "secondary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Tertiary

<Canvas>
  <Story
    name="Tertiary"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner variant="tertiary" id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "5",
      variant: "tertiary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Highlight

<Canvas>
  <Story
    name="Highlight"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner variant="highlight" id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "6",
      variant: "highlight",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Sizes

### Small

<Canvas>
  <Story
    name="Small"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner size="small" id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "7",
      size: "small",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Medium

<Canvas>
  <Story
    name="Medium"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner size="medium" id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "8",
      size: "medium",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Large

<Canvas>
  <Story
    name="Large"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner size="large" id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "9",
      size: "large",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### XLarge

<Canvas>
  <Story
    name="XLarge"
    decorators={[
      moduleMetadata({
        declarations: [LoadingSpinnerComponent, InstantUiLibraryComponent],
        imports: [CommonModule],
      }),
      componentWrapperDecorator(InstantUiLibraryComponent),
    ]}
    parameters={{
      docs: {
        source: {
          code: `<iu-loading-spinner size="xlarge" id="1"></iu-loading-spinner>`,
        },
      },
      layout: "centered",
    }}
    args={{
      id: "10",
      size: "xlarge",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
