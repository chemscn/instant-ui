export type ModeType =
  | Mode.PRIMARY
  | Mode.SECONDARY
  | Mode.TERTIARY
  | Mode.HIGHLIGHT;
export enum Mode {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
  HIGHLIGHT = 'highlight',
}
